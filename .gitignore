# macOS
.DS_Store
*.swp

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside

# XcodeGen-generated project (regenerated locally + in CI)
*.xcodeproj

# Bundled Ghostscript (built from source by scripts/build-ghostscript.sh; 26 MB — never committed)
Resources/ghostscript/

# Stray artefacts from the abandoned jbig2enc/leptonica experiment. Kept ignored so a machine
# that still has the tree from that attempt cannot accidentally commit or link against it.
Resources/native/

# Swift Package Manager
.build/
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata

# Claude tooling state (machine-local, never committed)
.headroom/
.desloppify/
.claude/worktrees/
.claude/settings.local.json

# Coverage artefacts from running instrumented binaries directly
*.profraw

# Build + packaging output
build/
dist/
