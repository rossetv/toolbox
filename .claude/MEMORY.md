<!-- Claude-maintained; humans never edit. Claude's own project memory, shared
with every contributor through git. Index only — one line per memory:
`- [title](memory/YYYYMMDD-<slug>.md) — one-line hook`. Bodies live in
memory/, registry-exempt in INDEX.md (this file is their registry; an orphan
either way is a defect); body shape: `# Title` + dense fact — why and
how-to-apply only when non-obvious. Injected verbatim every session — keep
rows to one line. Self-directed: record whatever a future session of ANY
contributor needs — behavioural invariants, core purpose and priorities,
operational tribal knowledge, standing authorisations and boundaries. Being
told "remember X" is sufficient, never necessary. Living store, not a log:
update in place, newest statement wins, delete when false; the filename date
is the creation date, never renamed. Exception: standing well-known files — today exactly review-lessons.md — are undated and updated in place. NEVER store: secrets/credentials/PII
(the repo reaches GitHub — cite a secret's location, never its value;
internal-only endpoints are fine); personal working-style preferences
(auto-memory); code-verifiable facts (KB docs); session decisions
(DECISIONS.md).
Hard byte ceiling: ≤4,000 B — shares its SessionStart block with decision titles and reminders under the 8,000 B per-block budget. -->

# Toolbox — Memory

- [AGPL distribution posture](memory/20260722-agpl-distribution-posture.md) — private dev, public at release, MAS foreclosed permanently, never write about the user's personal test PDFs.
- [Review lessons](memory/review-lessons.md) — standing checks from this repo's review rounds: sandbox profiles must be proven by running the binary, never walk a CGImage buffer flat, build the shared layer before the parallel fork, prefer an invariant fail-loud net to patching a parser under time pressure.
- [Public-release audit](memory/20260724-public-release-audit.md) — 2026-07-24 audit clean; all remaining identity disclosures (attribution, handle, author address, one historical corporate-email blob) explicitly accepted — don't re-flag.
